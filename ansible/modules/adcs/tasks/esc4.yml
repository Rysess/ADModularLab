---
# The template issues nothing useful; the writable ACL is the finding.
- name: Create the ESC4 template
  microsoft.ad.cs_template:
    name: "{{ adcs_esc4_template }}"
    display_name: "{{ adcs_esc4_display_name }}"
    source_template: Web Server
    schema_version: 2
    key_size: 2048
    extended_key_usages:
      - server_authentication
    validity_period_days: 365
    renewal_period_days: 42
    publish_to_ca:
      - "{{ adcs_ca_common_name }}"
    state: present
  delegate_to: "{{ adcs_dc_host }}"

- name: Grant full control over the ESC4 template
  ansible.windows.win_powershell:
    error_action: continue
    parameters:
      Template: "{{ adcs_esc4_template }}"
      Principals: "{{ adcs_esc4_writers }}"
    script: |
      [CmdletBinding()]
      param(
        [Parameter(Mandatory)][string]$Template,
        [Parameter(Mandatory)][string[]]$Principals
      )
      Set-StrictMode -Version Latest
      Import-Module ActiveDirectory
      $Ansible.Changed = $false

      $cfg = (Get-ADRootDSE).configurationNamingContext
      $path = "AD:\CN=$Template,CN=Certificate Templates,CN=Public Key Services,CN=Services,$cfg"
      $acl = Get-Acl -Path $path
      $dirty = $false

      foreach ($p in $Principals) {
          $obj = Get-ADObject -LDAPFilter "(sAMAccountName=$p)" -Properties objectSid
          if (-not $obj) { throw "No object with sAMAccountName '$p'" }
          $sid = $obj.objectSid

          $existing = $acl.Access | Where-Object {
              $_.AccessControlType -eq 'Allow' -and
              $_.ActiveDirectoryRights.HasFlag(
                  [System.DirectoryServices.ActiveDirectoryRights]::GenericAll) -and
              $_.IdentityReference.Translate(
                  [System.Security.Principal.SecurityIdentifier]).Value -eq $sid.Value
          }
          if ($existing) { continue }

          $acl.AddAccessRule(
              (New-Object System.DirectoryServices.ActiveDirectoryAccessRule(
                  $sid, 'GenericAll', 'Allow')))
          Write-Output "granted GenericAll on $Template to $p"
          $dirty = $true
      }

      if ($dirty) {
          Set-Acl -Path $path -AclObject $acl
          $Ansible.Changed = $true
      }
  delegate_to: "{{ adcs_dc_host }}"
